nodeLinker: node-modules

# First-party IWGF packages are consumed from npm here rather than as workspace
# links, so a release we just published is younger than npmMinimalAgeGate (24h).
npmPreapprovedPackages:
  - double-eighteen
  - double-eighteen-react
  - warp12-engine
  - warp12-react
  - warp12-theme
